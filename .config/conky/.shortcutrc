conky.config = {
    times_in_seconds = true,
    alignment = 'bottom_left',
    background = true,
    border_outer_margin = 25,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = white,
    default_outline_color = '#555',
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = true,
    double_buffer = true,
    extra_newline = false,
    font = 'Hack:size=10',
    gap_x = 50,
    gap_y = 95,
    no_buffers = true,
    out_to_console = false,
    out_to_stderr = false,
    own_window = true,
    own_window_class = 'Conky',
    own_window_transparent = true,
    own_window_argb_visual = false,
    own_window_argb_value = 150,
    own_window_colour = "#333",
    own_window_type = "override",
    own_window_hints = 'below',
    short_units = true,
    stippled_borders = 0,
    update_interval = 3,
    uppercase = false,
    use_xft = true,
    use_spacer = 'none',
    show_graph_scale = false,
    show_graph_range = false,
}

conky.text = [[
${color grey}colorpicker $color mod+shift+p
${color grey}prev workspace $color ctrl+mod+left
${color grey}next workspace $color ctrl+mod+right
${color grey}switch workspace $color mod+<number>
${color grey}fullscreen $color mod+f
${color grey}kill $color mod+shift+x

${color grey}terminal $color mod+enter
${color grey}chrome $color mod+b
${color grey}code $color mod+c
${color grey}spotify $color mod+m
]]
